# NeuroRegistry — Python dependencies
ladybug>=0.11.0,<0.18  # 0.18.x has no macOS arm64 wheel; pins to a version with prebuilt binaries
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
rdflib>=7.0.0
httpx>=0.27.0
pydantic>=2.0.0
click>=8.0.0
pyyaml>=6.0
# Exact pin, not a range: schema_registry_utils/models.py is generated from
# meta_model.yaml by scripts/gen_models.sh, and gen-pydantic bakes the
# metamodel version into its output. A different LinkML therefore produces
# different bytes for an unchanged schema, which fails `gen_models.sh --check`
# for reasons unrelated to the change under review. More importantly, the
# generator decides field optionality and defaults, and those feed
# compute_content_hash() — so a generator change could silently shift every stored
# sha256_hash. .github/workflows/gen_models.yml installs this exact line; bump it
# here and regenerate models.py in the same commit.
linkml==1.11.1
# X -> LinkML importers (JSON Schema now; OWL/SQL/etc. available for later).
# Compatible with the linkml==1.11.1 pin above (requires linkml<2,>=1.10.0).
# NOTE: pulls a large dependency tail (only the JSON Schema importer is used);
# its importer drops pattern/minimum/maximum, which converters/from_jsonschema.py
# re-applies in a post-process pass (upstream fix pending).
schema-automator>=0.5.7,<0.6
pytest>=8.0.0
# Upper bound matches pyproject.toml: mcp 2.0.0 dropped mcp.server.fastmcp,
# which neuro_ghost/mcp_server.py imports.
mcp[cli]>=1.0.0,<2.0.0
